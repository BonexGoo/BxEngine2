bekon_script_area<begin>

group(Bx)
{
	group(Currently) // 현재정보
	{
		string(Title) = "Test1"
	}
	group(Framework) // 프레임워크
	{
		string(FirstScene) = "Test-List"
		string(GUIScene) = "BxSimulator"
		group(GUIMargin)
		{
			number(Left) = 7
			number(Top) = 38
			number(Right) = 7
			number(Bottom) = 7
		}
	}
	group(Memory) // 메모리
	{
		group(Cache)
		{
			number(Sound) = 3000000
			number(VBO) = 16000
		}
	}
	group(WebContent) // 웹컨텐츠
	{
		number(CacheCountSumMax) = 500
		number(CacheSizeSumMax) = 10000000
	}
	group(VirtualScreen) // 가상화면
	{
		check(Enable) = false
		number(Width) = 0
		number(Height) = 0
	}
	group(Texture) // 텍스쳐
	{
		number(Margin) = 2
		number(Interpolation) = 1
	}
}

bekon_script_area<end>
